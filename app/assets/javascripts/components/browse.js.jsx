(function(root) {
  'use strict';
  root.Browse = React.createClass({
    render: function () {
      return (
        <div>
          This is the browse component.
        </div>
      );
    }
  });
}(this));
